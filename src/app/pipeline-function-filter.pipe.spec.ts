import { PipelineFunctionFilterPipe } from './pipeline-function-filter.pipe';

describe('PipelineFunctionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipelineFunctionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
