import { RdfNodeMappingDialogAnchorDirective } from './rdf-node-mapping-dialog-anchor.directive';

describe('RdfNodeMappingDialogAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new RdfNodeMappingDialogAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
