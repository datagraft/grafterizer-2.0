import { PropertyNodeDialogAnchorDirective } from './property-node-dialog-anchor.directive';

describe('PropertyNodeDialogAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new PropertyNodeDialogAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
