import { RdfPrefixManagementDialogAnchorDirective } from './rdf-prefix-management-dialog-anchor.directive';

describe('RdfPrefixManagementDialogAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new RdfPrefixManagementDialogAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
